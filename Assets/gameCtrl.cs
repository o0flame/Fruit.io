using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gameCtrl : MonoBehaviour {
	/*
	public GameObject player;

	public bool disablePlayerBullet = false;
	// Use this for initialization
	void Start () {

	}

	void disableEnemyBullet(){
		
	}

	void enableEnemyBullet(){}

	void disablePlayerBulletMethod(){
		
		if (player != null) {
			player.GetComponent<charSelection> ().disableBullet = true;
			player.GetComponent<charSelection> ().disableBullets ();
		}
	}

	void enablePlayerBulletMethod(){
		
		if (player != null) {
			player.GetComponent<charSelection> ().disableBullet = false;


		}
	}


	// Update is called once per frame
	void Update () {
		if (disablePlayerBullet == false) {
			enablePlayerBulletMethod();
		} else {
			//Debug.Log ("disablePlayerBulletMethod called");
			disablePlayerBulletMethod ();
		}

	}
	*/
}
